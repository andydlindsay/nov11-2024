<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All the Movies</title>
</head>
<body>
  <h1>All the movies!</h1>

  <% for (const movieId in movies) { %>
    <div style="border: 2px solid orange;">
      <h2>Title: <%= movies[movieId].title %></h2>
      <h3>Director: <%= movies[movieId].director %></h3>
      <h3>Year: <%= movies[movieId].year %></h3>
      <h3>Genre: <%= movies[movieId].genre %></h3>

      <form method="POST" action="/movies/<%= movieId %>/delete">
        <button type="submit">Delete</button>
      </form>

      <a href="/movies/<%= movieId %>/edit"><button>Edit</button></a>
    </div>
  <% } %>

</body>
</html>
